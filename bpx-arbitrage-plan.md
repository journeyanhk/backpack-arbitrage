# Backpack 资金费率套利交易脚本 Plan v0.2（细化版）

> 状态：待岛主审批 | 日期：2026-08-05 | 作者：小虾

## 一、策略概述

Delta 中性资金费率套利，单一方向：买入现货 + 做空永续合约。
所有开仓 / 平仓由岛主在前端手动触发，不自动交易。

## 二、岛主已确认项

| # | 确认项 | 结论 |
|---|---|---|
| 1 | 单笔 100U | 名义价值 100 USDC 等值的币 |
| 2 | 同时成交 | 两腿 maker 挂单 → 超时后未成交腿改进取价（见下） |
| 3 | 选币 | 每次手动指定，从抵押品 ∩ 永续合约列表选 |
| 4 | 现货订单类型 | 支持 Market + Limit（核实通过） |
| 5 | 触发方式 | 开仓/平仓全部手动指令，不自动交易 |
| 6 | 方向 | 仅做多现货 + 做空永续合约 |
| 7 | 操作界面 | 本地 Flask 前端 |

## 三、同时成交策略（细化）

### 3.1 手续费对比

| 腿 | maker | taker |
|---|---|---|
| 现货 | 0.08% | 0.10% |
| 合约 | 0.02% | 0.05% |

合约手续费远低于现货。优先保障同时性 > 省几 bp。

### 3.2 执行策略

拆单后，每笔 100U 对应现货+合约各一单，逐对执行：

1. 同时挂出该对的两腿（现货限价 maker + 合约 post_only maker）
2. 等待 N 秒（默认 10s）
3. 检查成交状态：
   - **两腿都成交** → 该对完成，继续下一对
   - **仅现货成交** → 合约腿撤单，改用进取限价（向盘口靠近 0.1%）重挂；再超时则改市价单
   - **仅合约成交** → 现货腿撤单，改用进取限价重挂；再超时则改市价单
   - **两腿都没成交** → 价格漂移了，撤单、重新定价、重挂
4. 每对最多重试 3 次

## 四、技术架构

### 4.1 整体

```
[前端 HTML] ← HTTP → [Flask API :5055] ← SDK → [Backpack]
```

前端发指令 → Flask 调 SDK → 返回结果 → 前端展示

### 4.2 后端（Flask）

端口：5055（不和 5050-5052 收益增强冲突，不占 5051 期权天眼）

核心 API：

| 路由 | 方法 | 功能 |
|---|---|---|
| /api/state | GET | 返回当前状态（余额、持仓、订单、费率） |
| /api/symbols | GET | 可选币种列表（抵押品 ∩ 永续合约 + 最新费率） |
| /api/open | POST | 开仓指令 {symbol, notional, leverage, offset, timeout} |
| /api/close | POST | 平仓指令 {symbol} |
| /api/cancel | POST | 撤销当前所有订单 |
| /api/logs | GET | 操作日志 |

### 4.3 前端（内嵌 HTML）

单页面，分三个区域：

| 区域 | 内容 |
|---|---|
| 顶部 | 状态栏：账户余额、总敞口、当前保证金率 |
| 左侧 | 操作面板：币种下拉（含实时费率）、名义金额、杠杆、偏移比例、超时秒数、开仓/平仓按钮、撤单按钮 |
| 右侧 | 数据面板：当前持仓、活跃订单、执行日志；底部资金费率信号摘要（复用 bpx_signal 扫描结果） |

设计：简洁白色主题，类似 us-options-buyer-strategy 的前端风格。

### 4.4 前端操作参数建议

| 参数 | 默认值 | 说明 |
|---|---|---|
| 币种 | — | 下拉框，从 /api/symbols 加载，显示"MON 53%周均"等 |
| 总名义金额 | 1000 USDC | 开仓总敞口 |
| 杠杆倍数 | 2x | 自动限制不超过该币种 IMF 最大杠杆 |
| 挂单偏移 | 0.05% | 相对参考价的 maker 偏移 |
| 超时秒数 | 10s | 单对等待成交时间 |
| 最大重试 | 3 | 单对最大重挂次数 |

操作按钮：开仓、平仓、撤单、刷新。下方实时操作日志。

## 五、文件结构

```
bpx-stock/
  bpx_arb_server.py      # Flask 后端（API + 交易引擎）
  templates/
    bpx_arb.html          # 前端页面
  bpx_signal.py           # 已有，信号扫描（复用数据）
  bpx_stock.py            # 已有，行情补丁
```

bpx_arb_server.py 模块：
- Flask app + 路由
- ArbitrageEngine：开仓/平仓编排、OrderSplitter 调用
- OrderSplitter：拆单 + 挂单 + 监控 + 撤单重挂
- PositionTracker：持仓查询、余额刷新

## 六、风控（开仓前校验 + 运行时监控）

| 项目 | 规则 |
|---|---|
| 杠杆上限 | 不超过该币种 collateral IMF（MON 3.3x，SOL 8x 等） |
| 余额校验 | 开仓前检查 USDC 余额 >= 所需名义 |
| 抵押权重校验 | 买入的现货按 haircut base 折算计入抵押品 |
| 资金费率门槛 | 开仓可选要求周均 >= 10%（前端提示，不硬拦） |
| API key 权限 | Read + Trading，不提现 |
| dry_run | 前端开关，开启时打印 + 不下单 |

## 七、开发步骤

1. 搭 Flask 骨架 + /api/symbols /api/state 两个只读端点
2. 写 OrderSplitter（拆单 + 挂单 + 监控 + 撤单重挂）
3. 写 ArbitrageEngine（编排开仓/平仓流程）
4. 写前端 HTML 页面
5. dry-run 全链路测试（打印不下单）
6. 配 API key 小额实盘验证

## 八、待岛主确认

1. 端口 5055 可以吗？
2. 前端风格：简洁白底 + 实时刷新，类似 us-options-buyer-strategy？
3. 开仓前做不做资金费率门槛检查（周均 <10% 警告但不拦）？
4. dry_run 开关放前端？
5. 日志保留在本地文件还是只看前端日志面板？