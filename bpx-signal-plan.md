# Backpack 资金费率套利信号器 Plan v0.1

> 状态：待岛主审批 | 日期：2026-08-05 | 作者：小虾

## 一、需求

1. 定时扫描抵押品目录中全部币种的永续合约资金费率。
2. 筛选一周平均年化 10% 以上的币种。
3. 以表格呈现：币种、现货杠杆、合约杠杆、最新资金费率（年化APY）、一周平均（年化APY）、一月平均（年化APY）。

## 二、数据来源

| 数据 | 端点 | 说明 |
|---|---|---|
| 抵押品列表 | GET /api/v1/capital/collateral | 在 19 个抵押资产中过滤，只取同时有永续合约的 |
| 永续合约列表 | GET /api/v1/markets（过滤 _PERP） | 与抵押品列表取交集 |
| 现货杠杆 | collateral 的 imfFunction.base | 1 / IMF_base |
| 合约杠杆 | 永续合约 market 的 imfFunction.base | 1 / IMF_base |
| 资金费率历史 | get_funding_interval_rates(sym, limit=N) | API 最多返回 500 条（约 20 天），每小时一条 |

## 三、计算逻辑

- 最新费率：最近一条 fundingRate × 24 × 365 × 100（年化 APY%）
- 一周平均：取最近 168 条（7 天 × 24h）的均值 × 24 × 365 × 100
- 一月平均：取最近 500 条（约 20 天）的均值 × 24 × 365 × 100。不足 500 条则用全部
- 筛选条件：一周平均年化 >= 10%
- 排序：一周平均年化从高到低

## 四、输出格式

终端表格，约 100 字符宽，每行列一个币种。

示例：
```
币种    现货杠杆  合约杠杆  最新(年化APY)    一周均(年化APY)  一月均(年化APY)
────    ────────  ────────  ──────────────  ───────────────  ───────────────
STRK    10x       10x        81.7%(29,803%)   65.2%(23,798%)   58.1%(21,207%)
MON     3.3x      10x       142.4%(51,976%)   78.3%(28,580%)   55.4%(20,221%)
...
```

括号内为年化 APY 百分数，括号外为原始费率（可选保留，也可以只显示年化）。

备选：加一个 HTML 看板输出（后期可扩展，第一期终端即可）。

## 五、筛选阈值

- 默认：一周平均年化 >= 10%
- 可通过命令行参数调整（--min-rate 10）
- 也可加"一月平均 >= 5%"作为交叉过滤（避免仅一周飙高的异常）

## 六、运行方式

- 单次扫描：`python bpx_signal.py`
- 定时扫描：`python bpx_signal.py --watch 300`（每 5 分钟刷新，Ctrl+C 退出）
- 输出到文件：`python bpx_signal.py --output signal.txt`
- Python 标准库即可，不动用 Rich/表格库；也可加 `tabulate` 美化（可选依赖）

表格刷新模式（--watch）：用 ANSI 清屏 `\033[2J\033[H` 原地刷新，模拟 TUI。

## 七、文件结构

```
bpx-stock/
  bpx_signal.py        # 信号器主脚本
  bpx_stock.py         # 已有，行情补丁（复用 PublicStock）
```

bpx_signal.py 职责：
1. 拉 collateral + markets 取交集
2. 并发拉各币种 fundingRates 历史
3. 计算最新/周均/月均
4. 过滤、排序、表格输出
5. 可选 watch 模式循环

## 八、开发步骤

1. 写 bpx_signal.py：数据拉取 + 计算 + 终端表格输出
2. 实测验证：对比真实数据确认杠杆率、年化计算正确
3. 加 --watch 定时刷新模式
4. 加筛选阈值参数
5. 交付 + 演示

## 九、待岛主确认

1. 表格里括号外还保留原始费率（如 0.00008098）还是只显示年化 APY？
2. 筛选条件：仅一周均 >= 10%，还是加一月均交叉过滤？
3. 是否需要输出到文件持久化？
4. 是否接受终端表格（第一期不做 HTML）？
5. 自选标的：是否只展示抵押品 ∩ 永续合约，还是增加自定义标的列表？
